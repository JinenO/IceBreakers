/* ============================================
   CONFIG - Global Settings
   ============================================ */
export const AppConfig = {
    // Scan speed (ms)
    SCAN_SPEED: 1500,

    // Sound toggle
    SOUND_ON: true,

    // Future MediaPipe threshold
    BLINK_THRESHOLD: 0.01
};
