/* ============================================
   CONFIG - Global Settings
   ============================================ */
export const AppConfig = {
    SCAN_SPEED: 2000,
    BLINK_THRESHOLD: 0.012,
    REQUIRED_BLINK_TIME: 800,
    SOUND_ON: true
};
